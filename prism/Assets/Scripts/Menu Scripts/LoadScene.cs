using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class LoadScene : MonoBehaviour
{
    // Literally all that is needed is to include UnityEngine.SceneManagement.
    // That gives a button the ability to call LoadScene when its OnClick() is
    // given an object with this script.

    // public string sceneName = "Main Menu";

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //void loadNewScene()
    //{
    //    SceneManager.LoadScene(sceneName, LoadSceneMode.Additive);
    //}
}
